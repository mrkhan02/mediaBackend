import os 
import pathlib 
import tempfile
from zipfile import ZipFile
mycwd = os.getcwd()

# a = mycwd.chdir("..")
# print(mycwd)
# print(a)

a = tempfile.gettempdir()
# print(a)
dir_list = os.listdir(os.path.join(mycwd, "static"))
# print(dir_list)

# for i in dir_list:
	# if os.

def get_all_dir(directory):
	file_paths = list()
	for root, directories, files in os.walk(directory):
		for filename in files:
			filepath = os.path.join(root, filename)
			file_paths.append(filepath)
	return file_paths

# print(get_all_dir(os.path.join(mycwd, "static")))

def main():
	directory = "static/js"
	# directory = os.path.basename(os.path.join(mycwd, "static"))
	# print(directory)
	file_paths = get_all_dir(directory)
	for file in file_paths:
		print(file)
	# name = "kl.zip"
	# zipname = os.path.join(a, name)
	# with ZipFile(zipname, "w") as zip:
	# 	for file in file_paths:
	# 		zip.write(file)

# main()
b = os.chdir("static/js")
print(b)